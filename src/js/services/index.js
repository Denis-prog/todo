import setFocusElement from './setFocusElement';
import {
    setCursorText,
    hiddenCursorText,
} from './textFieldCursorHandler';
import getTextField from './getTextField';
import getCurrentColumnId from './getCurrentColumnId';
import {
    showLoader,
    hiddenLoader,
    showError,
} from './loaderVisualHandler';
import {
    isNoteNew,
    removeNewNoteAttr,
} from './NoteOptionHandler';

export {
    setFocusElement,
    setCursorText,
    hiddenCursorText,
    getTextField,
    getCurrentColumnId,
    showLoader,
    hiddenLoader,
    showError,
    isNoteNew,
    removeNewNoteAttr,
};
